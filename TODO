	*	ctrl c inside heredoc wont run
	*	check for leaks inside increment shell level? cant find em
	*
	*	echo "'"$USER"'"
	*	export ok="k k"

Use for writing to stderr??
{
	void func(char *string[]);

	func((char *[]){"functon failed: ", test, "lkj"});
}

bash-3.2$ echo test > $TEST
bash: $TEST: ambiguous redirect

bash-3.2$ << "$TEST" cat
does not expand in heredoc

<< test --- segfault
<< test cat --- cat leaks

export hi="ls"
$hi
in Arguments:
-"ls"-
should be -ls-

echo "'"ls"'"
-echo-
-'ls"'"-
'ls"'"

ls				-la

	*	exit value check every function
	*	fix signals inside child
	*	pressing tab when using cd shows all files not directories
	*	check for leaks inside increment shell level? can't find em
	*
	*	check only whitespace input
	*	get tabs working
	*	expansion of non existent variable loops
	*	echo "'"$USER"'"
	*	export ok="k k"

Use for writing to stderr??
{
	void func(char *string[]);

	func((char *[]){"functon failed: ", test, "lkj"});
}

bash-3.2$ echo test > $TEST
bash: $TEST: ambiguous redirect

export hi="ls"
$hi
in Arguments:
-"ls"-
should be -ls-

echo "'"ls"'"
-echo-
-'ls"'"-
'ls"'"